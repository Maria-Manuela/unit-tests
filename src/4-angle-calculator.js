export const angleCalculator = (turns) => {
const degrees = turns * 360; //
  return degrees;
}

// the input to the function is a number of complete or fractional turns
//to get the angle in degrees, multiply number of turns by 360
